# Error Experience

Structured records of engineering errors, bugs, and anti-patterns.

- `entries/` — Individual dated entries (created via `/record error: ...`)
- `summary/` — Consolidated summaries (created via `/retro`)

## Entry Format
See `.claude/commands/record.md` for the standard entry template.
